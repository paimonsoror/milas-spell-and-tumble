{
  "name": "milas-spell-and-tumble",
  "private": true,
  "version": "1.0.0",
  "description": "Mila's Spell & Tumble Championship — build tooling only. The game itself ships as the esbuild output in dist/, no runtime dependencies.",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "test": "node tests/check.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
